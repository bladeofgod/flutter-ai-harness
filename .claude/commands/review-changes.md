---
description: 只读审查当前改动并输出问题报告，不修改实现
argument-hint: "[作用域或分支名]"
---

对当前改动执行只读 Review。可以读取文件、检查 diff 和运行非破坏性验证，也可以写 Review 报告；不得修改实现、测试、配置或依赖。

## 首轮审查

使用 `reviewer`。根据用户请求、任务卡或分支确定目标 diff，并保护无关工作树改动。

写入 `docs/reviews/<scope>.md`，按严重级别排序：

- P0：安全、数据丢失、崩溃、构建失败、严重正确性问题或不兼容公共/wire 契约，阻断完成。
- P1：高概率回归、架构越界、生命周期泄漏、缺少必需行为或关键测试缺口，阻断完成。
- P2：维护性或低风险质量建议，可以显式延后。

每条问题必须包含文件行号、影响、证据和明确修法。

若报告对应的旧问题已经被其他改动修复，可以在同一报告追加“复审”章节，但仍不得自行实施修复。

交付报告路径、各级问题、验证结果和剩余风险。需要修复时，等待用户明确调用 `/fix-review-findings <review-report-path>`。
