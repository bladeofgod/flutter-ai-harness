# 优先使用受支持的工程路径

生产代码优先使用平台和 SDK 的公开文档化 API。

- 能力不支持时，优先显式标记不支持、合理降级或请求产品决策。
- 私有 API、反射内部实现、修改三方二进制和依赖未文档化行为，都必须先说明维护/发布风险并获得明确同意。
- Spike 可以验证可行性，但实验 workaround 不会自动成为生产方案。
- 使用带正确可用性判断的官方新 API 不属于 Hack。

遇到阻塞时，准确说明官方能力能做什么、不能做什么。不得为了完成任务而静默替换成不同的产品行为。
