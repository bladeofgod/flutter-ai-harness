# IM 架构

> 状态：占位。当前仓库只有 `app_im` 包边界，尚无产品实现。

Demo 出现首个真实 IM 需求时，通过任务卡补充本文件。内容至少覆盖 Engine 接口、连接与鉴权生命周期、消息与事件模型、持久化边界、推送协作、错误恢复、测试策略和壳工程装配。

在此之前，以 `docs/architecture.md` 的依赖方向和 Domain Entity 边界为准，不预先设计没有消费者的 IM 抽象。
