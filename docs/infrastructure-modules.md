# 基础模块清册

> 状态：占位。清册随 Demo 的真实能力逐步形成。

本文件用于记录已经落入 `app_core`、`app_data`、`app_ui`、`app_im` 和 `app_rtc` 的公共能力、入口、消费者与限制。实现新能力前先检查本清册，避免在 Feature 中重复建设基础设施。

首次新增可复用基础模块时，记录模块路径、职责、公共 API、依赖方向、初始化方式、平台差异和验证命令。
