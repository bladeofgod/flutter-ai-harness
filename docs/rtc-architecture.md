# RTC 架构

> 状态：占位。当前仓库只有 `app_rtc` 包边界，尚无产品实现或已选定的 SDK 契约。

Demo 出现首个真实 RTC 需求时，通过任务卡补充本文件。内容至少覆盖会话状态、Bridge 契约、平台实现边界、权限、前后台生命周期、异常恢复、测试策略和壳工程装配。

在此之前，以 `docs/architecture.md` 和 `docs/bridge/README.md` 为准，不绑定未选定的 RTC 厂商或私有依赖。
